apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deployment-digest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-digest
  template:
    metadata:
      labels:
        app: web-digest
    spec:
      containers:
      - name: nginx
        image: harshpanchal18/nginx-argo:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"